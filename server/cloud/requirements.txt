fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ─── Опционально: RAG (RAG_ENABLED=true) ──────────────────────────────
# Cloud-сервер использует shared/rag_store.RagStore с Ollama-эмбеддером.
# Если RAG_ENABLED=false (дефолт), эти зависимости не нужны.
# Установите при сборке RAG-индекса:
# python-docx>=1.1.0      # парсинг .docx
# pypdf>=4.2.0            # парсинг .pdf
# beautifulsoup4>=4.12.0  # парсинг .html

# v2.2: убраны pymorphy3 / pymorphy3-dicts-ru / natasha — морф-фильтр,
# морф-детектор, sage и LanguageTool больше не используются в cloud
# (сетевая модель сама лучше справляется с этими классами ошибок).
