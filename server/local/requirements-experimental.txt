# Dependencies for presets D/F. Installed once with the common server requirements.
# Qwen3.5-4B was released against the 4.57 Transformers API; keep the
# major version pinned so PEFT adapter module names stay compatible.
transformers>=4.57.0,<5.0
accelerate>=1.7.0
peft>=0.19.1
safetensors>=0.5.3
torch>=2.2.0
huggingface_hub>=0.30.0
sentencepiece>=0.2.0
