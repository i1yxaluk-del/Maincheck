# Runtime dependencies for the local GEC cascade.
transformers==5.16.1
peft>=0.18.0
accelerate>=1.13.0
safetensors>=0.7.0
torch>=2.5.0
huggingface_hub>=1.10.0
sentencepiece>=0.2.0
# Natasha still imports pkg_resources on the current production image.
setuptools==81.0.0
