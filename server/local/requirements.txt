fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Для RAG-очистки документов (опционально, но рекомендуется)
python-docx>=1.1.0
pypdf>=4.2.0
beautifulsoup4>=4.12.0

# v1.7: морфологический фильтр галлюцинированных «улучшений» падежных
# форм (см. server/shared/morph_filter.py). Без этих пакетов сервер
# работает, но фильтр будет отключён — закрытый раньше в v1.6.10
# bypass «Подразделения → Подразделению» снова возможен.
# pymorphy3 — pure Python (не требует gcc), ~2 МБ; pymorphy3-dicts-ru
# — словарь OpenCorpora ~50 МБ. Полностью offline после установки.
pymorphy3>=2.0.0
pymorphy3-dicts-ru>=2.4.0

# v1.8c: sage-95m post-валидатор (опционально, default OFF).
# `ai-forever/sage-fredt5-distilled-95m` — компактная русская GEC-модель,
# проверяет каждую правку T-lite как «второе мнение». Включается через
# SAGE_VALIDATOR_ENABLED=true в .env. Если transformers/torch не
# установлены, валидатор отключается автоматически (no-op) — сервер
# работает без него. Не делаем эти пакеты required, потому что они
# тяжёлые (≈1.5 ГБ install) и не всем нужны: админ-only установки
# могут жить на morph-детекторе v1.8a.
# Раскомментируйте если планируете включить SAGE_VALIDATOR_ENABLED:
# transformers>=4.40
# torch>=2.2
# huggingface_hub>=0.23
# sentencepiece>=0.2  # для T5-токенизатора
