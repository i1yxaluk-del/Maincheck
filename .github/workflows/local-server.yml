name: local-server

on:
  pull_request:
  push:
    branches: [main]

jobs:
  python-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server/local
    env:
      PYTHONPATH: ${{ github.workspace }}/server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install lightweight test deps
        run: python -m pip install --upgrade pip pytest httpx pymorphy3 pymorphy3-dicts-ru
      - name: Compile application
        run: python -m py_compile decision_app.py decision_engine.py hybrid_editor.py qwen35_backend.py ollama_gec.py russian_gec_backend.py russian_mlm_corrector.py russian_quality_models.py safe_diff.py syntax_candidates.py local_rules.py
      - name: Run v7 architecture regressions
        run: pytest -q test_v7_gec.py
      - name: Run v6 architecture regressions
        run: pytest -q test_v6_cascade.py
      - name: Run v5 architecture regressions
        run: pytest -q test_v5_cascade.py
      - name: Run v3.1 deterministic regressions
        run: pytest -q test_v31.py
      - name: Run hybrid regressions
        run: pytest -q test_hybrid_editor.py
      - name: Run legacy regressions
        run: pytest -q test_pipelines.py
