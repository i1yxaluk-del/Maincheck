# Журнал v1.6 — Retrieval-augmented few-shot для русского GEC

## Контекст

После ряда серверных фиксов v1.5.8–v1.5.11 (терминатор `===END===`, фильтр
«…»-цитат, валидация «было», реконструкция CHANGES из diff) оставалась
корневая проблема: **ни одна из 11 моделей пула не справляется с
структурной пунктуацией длинных юридических конструкций** (эталонный
текст Росгвардии, 4 известные ошибки обособления — 0-2 из 4 находятся).

Первоначальная гипотеза (см. `АНАЛИЗ_МОДЕЛЕЙ_2026_05.md`): перейти
на специализированный GEC-корректор — SAGE FRED-T5 от ai-forever.

**PoC опроверг гипотезу.** На тексте Росгвардии (30 апреля 2026):

| Модель | Латентность | Запятые найдены | Галлюцинации |
|---|---|---|---|
| `sage-fredt5-distilled-95m` (95M) | 3.2 с | 0 / 3 | «2025» → «2015», пустая «» |
| `sage-fredt5-large` (1.7B) | 22.5 с | 0 / 3 | «2025» → «2015», «ТопСервис» → «Топ-Сервис» |
| `sage-mt5-large` (1.2B) | 49 с | 0 / 3 | «2025» → «2015» |
| `sage-m2m100-1.2B` (1.2B) | 46.5 с | 0 / 3 | — (но и правок нет) |

Выводы:
1. SAGE обучена на типо- и орфо-ошибках из Wikipedia/видео-транскриптов,
   а не на структурную пунктуацию обособления. Другая задача.
2. SAGE стабильно галлюцинирует числа («2025 год» → «2015 год»).
   Для юр.документа — критичная ошибка.
3. F1 paper-метрик (60 %) получены на RUSpellRU, а не на нашем домене.
   Academic benchmark ≠ prod.

## Новая гипотеза (v1.6)

Проверил Sorokin & Nasyrova (BEA @ ACL 2025, LORuGEC paper). Их
эмпирические цифры на русском GEC:
- 0-shot LLM (T-lite / YandexGPT): **~55 % F0.5** (то что у нас сейчас)
- 5-shot LLM + GECToR retrieval: **83 % F0.5 — SOTA**
- Fine-tune seq2seq: 65–75 % F0.5 (хуже retrieval-augmented)

Retrieval лучше fine-tune. Ещё и почти бесплатно.

## Архитектура v1.6 Этап 1 (реализована)

```
ПОТОК:
    request (raw_text + context)
        ↓
    _rag_context(raw_text)           # старый RAG, без изменений
        ↓
    [NEW] _gec_bank.search(raw_text, top_k=3)  # FAISS-like в numpy
        ↓ [top-3 похожих пар из банка]
    [NEW] build_few_shot_messages(   # system + N × (user, assistant) + user
              SYSTEM_PROMPT, user_msg, examples
          )
        ↓
    call_ollama(messages)            # тот же T-lite-it-2.1
        ↓
    ===CORRECTED=== / ===CHANGES===  # тот же формат
        ↓
    _drop_idempotent_changes  ← оставили 1.5.11 как есть
    _drop_changes_not_in_text ← оставили 1.5.11 как есть
    _rebuild_changes_from_diff ← оставили 1.5.11 как есть
        ↓
    client
```

### Файлы

- `server/shared/gec_bank.py` — новый модуль (`GecPair`, `GecBank`,
  `build_few_shot_messages`). 210 строк.
- `server/shared/gec_seed/gec_bank.jsonl` — seed-банк 288 пар из LORuGEC,
  сбалансирован по 48 правилам (≤ 6 примеров на правило). Размещён
  в `shared/gec_seed/`, а не `local/data/`, потому что `data/` глобально
  в `.gitignore`.
- `server/local/main.py` — добавлены env-флаги `USE_FEW_SHOT`,
  `GEC_BANK_FILES`, `GEC_TOP_K`, интеграция в `/suggest`,
  поля в `/health` и `/metrics`.
- `server/local/.env.example` — секция Few-shot retrieval с инструкцией
  и ссылкой на исходник.
- `tests/test_gec_bank.py` — 8 тестов (load/skip/search/build-messages/
  seed-sanity).

### Флаги

| Флаг | Default | Смысл |
|---|---|---|
| `USE_FEW_SHOT` | `false` | Мастер-флаг. Пока выкл — поведение v1.5.11. |
| `GEC_BANK_FILES` | `data/gec_bank.jsonl` | Список JSONL (через запятую). |
| `GEC_TOP_K` | `3` | Сколько примеров подмешивать. |

### Эмбеддер

Переиспользуем RAG-эмбеддер (если `RAG_ENABLED=true`) — один
`OllamaEmbedder(nomic-embed-text)` на оба индекса. Если RAG выключен,
создаём свой Ollama-эмбеддер; если Ollama недоступен — падаем
в `HashingEmbedder(dim=1024)` (лексический fallback без сети,
качество ниже, но сервер не ломается).

## Источник seed-банка и лицензирование

- Данные: LORuGEC (Sorokin & Nasyrova 2025, BEA @ ACL 2025),
  https://github.com/ReginaNasyrova/LORuGEC
- Формат исходника: XLSX, 961 строка, 48 правил,
  колонки «Initial sentence» / «Correct sentence» / «The rule» / …
- Обработка (см. `/tmp/build_seed_bank.py`, коммитить не нужно):
  - оставлены только пары, где `Are both sentences the same? == No`
  - детокенизация (убран пробел перед пунктуацией)
  - баланс `≤ 6 пар на правило` (чтобы банк не был перекошен)
  - 288 пар на выходе
- Цитирование обязательно (описано в `.env.example` и docstring модуля).

## Что НЕ сделано в этом релизе (deferred)

1. **Миграция на llama.cpp** — отдельная v1.6.1. Ortogonal от retrieval:
   retrieval уже даёт +20–30 % качества, llama.cpp добавит −35 %
   латентности. Сперва смотрим на качество, потом ускоряем.
2. **Интеграция ведомственных документов в банк** — точка расширения
   готова (добавить путь в `GEC_BANK_FILES` → `build_index`). Ждём
   корпус от заказчика.
3. **Бенчмарк на LORuGEC-test (1945 пар)** — скрипт отдельно, не
   блокирует PR. Acceptance criteria по paper: F0.5 ≥ 70 % (vs
   ~55 % в 0-shot).
4. **Проверка на Росгвардии с USE_FEW_SHOT=true** — требует реального
   Ollama на прод-машине, делается после мержа PR.

## Ожидаемые acceptance criteria

На момент релиза (acceptance на проде):

1. `USE_FEW_SHOT=false` — **побайтово** то же поведение, что в v1.5.11.
   Гарантируется: код пути few-shot изолирован, `messages` строится
   старой веткой.
2. `USE_FEW_SHOT=true` — на тексте Росгвардии: **≥ 3 из 4**
   эталонных запятых обособления находятся. (Сейчас 0–2.)
3. Warm-латентность — без регрессий. 3 примера × ~150 токенов =
   +450 tokens в промпте → при `num_ctx=2048` запас остаётся,
   `num_predict` не меняется, ожидаем +3…5 с к warm (45 → 48–50 с).
4. Формат `===CORRECTED===/===CHANGES===/===END===` — стабилен.
   Каждый из 3 assistant-примеров в истории демонстрирует правильный
   формат, модель имитирует.
5. Никаких новых галлюцинаций. Все предыдущие защиты
   (`_drop_changes_not_in_text`, `_rebuild_changes_from_diff`) активны.

## Хронология

| Дата | Событие | Ссылка |
|---|---|---|
| 30 апр 2026 | АНАЛИЗ_МОДЕЛЕЙ_2026_05.md — SAGE рекомендован | — |
| 30 апр 2026 | PoC SAGE FRED-T5: 0/3 запятых, галлюцинации чисел | `/home/ubuntu/poc_sage.py` |
| 30 апр 2026 | Пивот: LLM + few-shot retrieval, план v1.6 | `/home/ubuntu/ПЛАН_v1.6.md` |
| 30 апр 2026 | Ветка `devin/1777878847-v1.6-retrieval` | — |
| 30 апр 2026 | PR v1.6.0-rc1 (retrieval на Ollama) | TBD |

## Что впереди (после PR)

1. **Бенчмарк Росгвардия + LORuGEC-test** — точечный прогон с
   `USE_FEW_SHOT=true/false`, side-by-side.
2. **v1.6.1: миграция на llama.cpp** — target=T-lite-it-2.1 +
   draft=Qwen3-0.6B через speculative decoding, ожидание warm ~30 с.
3. **v1.6.2 (когда придёт корпус): ведомственный банк.**
   Формат: `{"wrong": "<до вычитки>", "right": "<утверждённая>",
   "rule": "ведомственный стиль"}`. Добавляется в `GEC_BANK_FILES`,
   пересобирается индекс (секунды), работает без обучения модели.

---

# Журнал v1.6.1 (5 мая 2026) — диагностика NUMA + расширение GEC-банка

## NUMA-фикс (главная победа сессии)

При тестировании 8B (`t-tech/T-lite-it-2.1`) и 4B (`Vikhrmodels/QVikhr-3-4B`)
на прод-сервере latency была катастрофической — простое `ollama run "Привет"`
занимало **1м37с** на 4B-модели, а `/suggest` упирался в таймауты 300-900 с
даже при 0-shot.

`ollama run --verbose` показал:
- `prompt eval rate`: **0.66 tok/s** (ожидаемо: 30-60)
- `eval rate`: **0.07 tok/s** (ожидаемо: 5-7)

Корень: VMware-виртуалка с 2 NUMA-нодами по 16 ядер/16 ГБ; Ollama в systemd
запущена через `numactl --cpunodebind=0 --membind=0` — половина железа
отрезана, плюс жёсткий NUMA-bind в гостевой ОС конфликтует с расписанием
vCPU гипервизором.

**Фикс — заменить `/etc/systemd/system/ollama.service.d/override.conf`:**

```ini
[Service]
ExecStart=
ExecStart=/usr/local/bin/ollama serve
Environment="OLLAMA_KEEP_ALIVE=24h"
Environment="OLLAMA_NUM_PARALLEL=1"
```

```bash
sudo systemctl daemon-reload
sudo systemctl restart ollama
```

После фикса:
- `prompt eval rate` 0.66 → **58 tok/s** (88×)
- `eval rate` 0.07 → **5.29 tok/s** (75×, для 4B); 9.22 tok/s для 8B
- `ollama run "Привет"` 113 с → **1.79 с**

## Latency после фикса (T-lite 8B + retrieval)

| Тест | Latency | Найдено эталонных ошибок (4 шт.) |
|---|---|---|
| 0-shot | 74 с | 4/4 (2 точно + 2 валидной альтернативой) |
| 1-shot (top_k=1) | 75 с | 4/4 |
| 3-shot (top_k=3) | — | не тестировали (1-shot достаточно) |

**Цель «70-80 с с лучшим качеством» достигнута на T-lite 8B без llama.cpp.**

## Расширение GEC-банка

Существующий `gec_bank.jsonl` — 288 пар (по 6 примеров на правило × 48 правил).
В тесте Росгвардии retrieval не находил релевантных пар на ошибку
`должностного лиц → должностных лиц` (Grammar/число), потому что в банке
было только 24 grammar-пары на 4 правила.

**Расширение через `tools/build_gec_bank/build.py`:** парсим
полный LORuGEC.xlsx (960 пар) → `gec_bank_extended.jsonl` (639 новых пар,
не пересекаются с базовым банком). Итого после подключения двух файлов:

| Раздел | Базовый | Расширенный | Итого |
|---|---|---|---|
| Spelling | 150 | 317 | 467 |
| Punctuation | 102 | 235 | 337 |
| Grammar | 24 | 55 | 79 |
| Semantics | 12 | 32 | 44 |
| **Всего** | **288** | **639** | **927** |

**Latency-импакт расширения банка: 0%.** Размер банка не влияет на /suggest
runtime — мы достаём top_k (=1) ближайших примеров из любого по размеру
пула. Изменяется только время первичной индексации (linear по объёму, потом
disk-cache из v1.6.0).

## v1.6.2 PR (этот)

- `tools/build_gec_bank/build.py` + `README.md` — скрипт сборки, идемпотентный.
- `server/shared/gec_seed/gec_bank_extended.jsonl` — 639 новых пар.
- `server/local/.env.example` — `GEC_BANK_FILES` теперь по умолчанию
  подключает оба файла; `GEC_TOP_K=1` (1-shot — баланс качество/время).

## Что впереди (Phase 2 и 3)

- **Phase 2 — лучший эмбеддер.** `multilingual-e5-large` или
  `sbert_large_nlu_ru` вместо `nomic-embed-text` для retrieval. Поиск
  релевантных пар станет точнее, особенно для длинных юр.предложений.
  PR — отдельный, поверх этого.
- **Phase 3 — каркас Document RAG.** Подсистема для будущих ведомственных
  документов из Гарант/Консультант. Чанкирование, очистка технического
  текста, Faiss-индекс. Ингест документов — позже (когда появятся);
  скаффолд готовится сейчас.
- **Phase 4 (отменено).** LoRA-файнтюн T-lite требует выгрузку обучающих
  данных на арендованный GPU. Ведомственные документы не должны покидать
  локальный сегмент → опция исключена. Если в будущем появится локальный
  GPU — возвращаемся.

## v1.6.3 — Конфигурируемый эмбеддер для GEC-банка

Phase 2 в виде маленького аддона: новая переменная `GEC_EMBED_MODEL`,
по умолчанию равна `RAG_EMBED_MODEL` (т.е. `nomic-embed-text`). Идея —
позволить администратору безопасно тестировать другой эмбеддер на
GEC-банке, не трогая RAG-документы (для длинных регламентов nomic был
выбран осознанно по объёму контекста, и менять его на каждой итерации
не хочется).

Fingerprint кэша индекса включает имя эмбеддера → автоматическая
переиндексация при смене модели; старый кэш остаётся на диске, и
откат — это снять переменную и рестартнуть.

Подкреплено документацией в `.env.example` и обновлёнными тестами
`test_gec_bank.py`.

## v1.6.4 — Скаффолд Doc-RAG (Phase 3)

Подсистема под будущую загрузку ведомственных документов готова
к ингесту, когда документы прибудут:

- `server/shared/rag_cli.py` — команды `add`, `list`, `remove`,
  `ingest-folder`, `search`, `preview` (dry-run очистки), `stats`
  (сводка по размеру, эмбеддеру, возрасту индекса).
- `server/shared/rag_store.py` — local-only хранилище без chromadb/faiss
  (numpy + JSON), self-contained формат.
- `server/shared/garant_cleanup.py` — стрипы Гарант/КонсультантПлюс
  (шапки, копирайты, оглавления с отточиями, мягкие переносы и т.д.).
- `/metrics` теперь показывает RAG-блок: количество документов, чанков,
  возраст индекса, имя эмбеддера.

Сама загрузка документов — на стороне администратора, как и было
заложено в плане. Phase 3 — это инфраструктура, не контент.

## v1.6.5 — Включить few-shot по умолчанию

После v1.6.2-v1.6.4 банк, эмбеддер и метрики стабильны.
`USE_FEW_SHOT=true` в `.env.example` — новые установки сразу получают
1-shot retrieval без шага «потом включите вот этот флаг».

## v1.6.6 — bge-m3 как эмбеддер по умолчанию для GEC

**Прод-валидация (5 мая 2026, ext, T-lite-it-2.1:q4_K_M, банк 894 пары
после фильтра вложенных кавычек):**

| Тест | nomic-embed-text (v1.6.5) | bge-m3 (v1.6.6) |
|---|---|---|
| Эталонных правок найдено | 4 / 4 | 4 / 4 |
| Из них **точно как эталон** | 2 / 4 | **4 / 4** |
| Latency 1-shot, top_k=1 | 63-71 с | 50-71 с |
| Размерность эмбеддинга | 768 | 1024 |
| Время первичной индексации 894 пар | ~3-4 мин | ~7-8 мин |

Что починилось:
- `должностного лиц` → `должностного лица` теперь правится точно
  (раньше — альтернативной формулировкой `должностных лиц` мн.ч.).
- `на объекте, курирования` → `на объекте, курируемом` теперь точно
  (раньше — альтернативной перефразировкой `на курируемом объекте`).

Любопытное наблюдение: retrieval всё ещё подмешивает не идеально
релевантную пару — `Склонение количественных числительных` — но
качество правок при этом эталонное. Значит, даже частичное «попадание»
few-shot уже даёт модели нужный сигнал по структуре ответа и
стилистике правок. Идеального retrieval (пара про согласование числа
существительного) — задел на следующие итерации, но текущий результат
уже эталонный.

**Изменение в репозитории:**
- `server/local/.env.example` — `GEC_EMBED_MODEL=bge-m3` теперь
  активный (раньше был закомментирован), nomic остаётся как
  fallback на случай отсутствия bge-m3.

**Действия на проде при следующем деплое:**
- Если `GEC_EMBED_MODEL=bge-m3` уже выставлен (как сделано вручную
  5 мая) — ничего не меняется, синхронизация `.env` с `.env.example`
  даст тот же результат.
- Если новая установка — обязательно `ollama pull bge-m3` (~600 МБ)
  и рестарт. Первичная индексация в фоне ~7-15 мин на CPU Broadwell,
  потом disk-cache из v1.6.0.

## v1.6.7 — Гибридный retrieval (BM25 + dense, RRF) + анти-галлюцинационный SYSTEM_PROMPT

**Контекст.** Прод-тест 5 мая 2026 на втором, более длинном
административном тексте (контракт КС-2, сметы) показал регресс
качества при той же конфигурации (T-lite-it-2.1:q4_K_M, USE_FEW_SHOT=true,
GEC_TOP_K=1, GEC_EMBED_MODEL=bge-m3, банк 894 пары):

- **Найдено эталонных правок: 1 / 4** (вместо 4/4 на предыдущем тесте).
- **+2 галлюцинации:** `Проведенными → Проведёнными` (е/ё стилистика, не
  ошибка) и `Подразделения → Подразделению` (модель «улучшила»
  грамматически правильное слово).
- Few-shot retrieval подмешал нерелевантную пару `Запятая перед
  союзом «как»` вместо нужных `Согласование причастий` /
  `Согласование числа существительных` — пары с такими правилами
  в банке есть (76 шт.), но bge-m3 их не выбирает на длинном входе
  с предметной лексикой (КС-2, государственный контракт, млн рублей).

**Корневая причина** (Sorokin & Nasyrova, BEA @ ACL 2025, та же группа,
что собирала LORuGEC): *«similarity between input texts does not
necessarily correspond to similar grammatical error patterns»*. На
длинных официальных текстах семантическое сходство (которое и
оптимизирует dense embedder) уводит ранжирование от грамматического
сигнала. Их рецепт — заменить dense на edit-based encoder (GECToR-like).
Альтернатива от Tang et al. (NAACL 2024) — двухстадийный retrieval:
BM25 для отбора кандидатов, синтаксический re-rank для финального
выбора. Производственная практика 2026 (tim-ponomarev/hybrid-rag,
legaltech RU): RRF dense+sparse даёт +27 % nDCG@10 на правовом домене.

### Что сделано в v1.6.7

1. **Hybrid retrieval RRF(BM25 + bge-m3 cosine)** — `server/shared/gec_bank.py`:
   - Чистый Python BM25Okapi (k1=1.5, b=0.75, ~30 строк, без новых
     зависимостей). Индекс строится в RAM при старте (~200 мс для
     894 пар, ~6 тыс. уникальных токенов, ~2-5 МБ памяти). Disk-cache
     не нужен.
   - Токенизация без лемматизации: ловим словоформы напрямую,
     потому что грамматическая ошибка как раз в словоформе
     («стоимостей» vs «стоимости», «выполненной» vs «выполненных»).
     Если стеммировать — потеряем главный сигнал.
   - Reciprocal Rank Fusion (Cormack et al. 2009, k=60) объединяет
     ранги двух систем без подкручивания шкал.
   - Новый env `GEC_RETRIEVAL_MODE=hybrid|dense|sparse` (дефолт
     `hybrid`). `dense` — поведение v1.6.6 для отката; `sparse` —
     для ablation-тестов.
   - Latency-impact: +5-15 мс на запрос (BM25 поиск по 894 парам
     <3 мс, RRF — микросекунды). Никакого +15-25 с от GEC_TOP_K=2.

2. **Анти-галлюцинационный гард в SYSTEM_PROMPT** — `server/local/main.py`:
   - Категориальные хинты по классам ошибок согласования: число
     существительного и зависимого прилагательного/причастия;
     причастный оборот после существительного с дальним антецедентом;
     `должностного лица` (ед.ч.) vs `должностных лиц` (мн.ч.).
   - Запрет на стилистические замены: е/ё взаимозаменяемы, не
     заменять `проведенными → проведёнными`.
   - Запрет «улучшать» грамматически правильное слово: если падеж
     и число корректны — не переписывать.
   - Принцип: **лучше пропустить ошибку, чем сочинить правку**.
   - Latency-impact: ~+0.3-0.5 с (промпт длиннее на ~600 символов).

3. **Прозрачность** — `/metrics` теперь возвращает
   `few_shot_retrieval_mode` и `few_shot_bm25_terms`. Логи запуска
   показывают `mode=hybrid|dense|sparse`. На каждый запрос лог
   формата `Few-shot (hybrid): подмешиваю N пар: [...]`.

4. **Тесты** — `tests/test_gec_bank.py`:
   - `_BM25Index` (3 теста: known-doc score, unknown term, empty).
   - `_tokenize_ru` (lower, ё→е, отбрасывает пунктуацию).
   - `search_sparse` (находит лексически близкие пары, пуст при отсутствии overlap).
   - `search_hybrid` (RRF top-2, fallback на dense без BM25, пустой банк).
   - Регрессионный тест на длинном админ-входе (синтетический банк, hybrid → правильная пара top-1).

### Прогноз по поведению

| Сценарий | Поведение v1.6.6 | Поведение v1.6.7 |
|---|---|---|
| 5 мая Росгвардии (короткий текст с явными ошибками) | 4/4 точно, 50-71 с | Должно быть **≥ 4/4 точно**: BM25 на коротких лексических совпадениях не хуже dense, плюс категориальные хинты убирают шанс е/ё-галлюцинации |
| 5 мая контракт КС-2 (длинный текст, причастные обороты) | 1/4 + 2 галлюцинации, 65 с | Прогноз **3-4/4 + 0 галлюцинаций**: SYSTEM_PROMPT блокирует обе галлюцинации, hybrid retrieval с большей вероятностью подмешает релевантные пары о согласовании причастий и числа существительного |
| Latency на обоих тестах | 50-71 с | Те же 50-71 с (дельта <1 с от BM25 + длинного промпта) |

### Действия на проде при следующем деплое

1. `git pull && git checkout main`
2. `sudo systemctl restart ai-suggester`
3. Проверить лог запуска:
   ```
   Few-shot retrieval включён: 894 пар, top_k=1, embedder=ollama:bge-m3, mode=hybrid
   GEC bank: BM25 sparse-индекс собран (894 пар, словарь N уникальных токенов)
   ```
4. Проверить `/metrics`: `few_shot_retrieval_mode: "hybrid"`,
   `few_shot_bm25_terms > 0`.
5. Прогнать оба тестовых текста — измерить latency и точность,
   зафиксировать в этом журнале.

### Откат, если hybrid вдруг хуже на каком-то корпусе

```bash
echo 'GEC_RETRIEVAL_MODE=dense' >> ~/llama/server/local/.env
sudo systemctl restart ai-suggester
```

## v1.6.7 — прод-валидация и обнаруженные проблемы

**Прод-тест 5 мая 2026, ext, T-lite-it-2.1:q4_K_M, КС-2 текст:**

| Эталонная правка | v1.6.6 | v1.6.7 |
|---|---|---|
| `стоимостей → стоимости` (число существительного) | пропуск | пропуск |
| `выполненной → выполненных` (причастие) | пропуск | **✓ точно** |
| `работ, путём → работ путём` (запятая) | **✓** | пропуск |
| `не предусмотренной → не предусмотренных` (причастие) | пропуск | **✓ точно** |
| **Точно** | **1/4** | **2/4** |
| Галлюцинации | 2 (Проведёнными, Подразделению) | 3 (+ повлёкших) |
| Latency | 65 с | **87 с (+22 с)** |

**Что v1.6.7 СДЕЛАЛ хорошо:**
- Категориальные хинты в SYSTEM_PROMPT научили модель ловить
  причастные обороты с дальним антецедентом (`выполненной → выполненных`,
  `не предусмотренной → не предусмотренных`).

**Что v1.6.7 СДЕЛАЛ плохо:**
1. **Latency +22 с.** SYSTEM_PROMPT вырос с 750 до 2680 chars =
   +600 токенов в промпте. На T-lite на CPU prompt eval ~30 ms/token =
   +18-22 с. Прогноз PR (`+0.3-0.5 с`) был ошибочным.
2. **Анти-галлюцинационные правила в промпте НЕ сработали.** Все 3
   галлюцинации (е/ё в `Проведёнными`/`повлёкших`, падежная подмена
   `Подразделения → Подразделению`) остались. Гипотеза: lost-in-the-middle —
   модель видит правило в начале длинного промпта и забывает его к
   моменту генерации.
3. **Hybrid retrieval НЕ помог на этом тексте.** Подмешал
   нерелевантную пару `Склонение количественных числительных` вместо
   нужных согласований. Hybrid-механика работает корректно (в логах
   `Few-shot (hybrid): подмешиваю 1 пар`), но на длинных текстах ни
   dense, ни sparse не вытащили нужные пары — они в банке есть, но
   их семантика и лексика далеки от КС-2 текста.

## v1.6.8 — Откат раздутого SYSTEM_PROMPT + серверный фильтр ё-замен

**Стратегия:** запрет соблюдается там, где соблюдение надёжно — на
уровне сервера, после генерации. Промпт сжимаем обратно к v1.6.6
базе, но сохраняем ОДИН компактный хинт про причастные обороты —
именно он дал +1 точную правку в v1.6.7.

### Что сделано

1. **SYSTEM_PROMPT сжат с 2680 до 1631 символа** (-1050 chars,
   -250 токенов, -8-10 с latency на T-lite на CPU).
   Сохранена единственная компактная строка о согласовании:
   *«согласование — однородные члены и причастные обороты в одном
   роде, числе и падеже с главным словом, даже если оно стоит за
   несколько слов»*. Эта одна строка и дала прирост в v1.6.7,
   остальной балласт убран.

2. **Серверный фильтр стилистических ё-замен** —
   `_drop_eyo_substitutions(text, raw_text)` в `server/local/main.py`:
   - Распознаёт пункты CHANGES, где `before` и `after` отличаются
     ИСКЛЮЧИТЕЛЬНО подменой ё↔е (или Ё↔Е): `проведенными ↔
     проведёнными`, `повлекших ↔ повлёкших`, `Тёркин ↔ Теркин`.
   - Удаляет такой пункт из `===CHANGES===`.
   - Откатывает подмену в `===CORRECTED===`, восстанавливая
     оригинальное написание из `raw_text`.
   - Срабатывает только если `before` есть в `raw_text` (т.е. правка
     реально была применена), не ломает редкие случаи с именами.
   - Применяется ДО `_drop_changes_not_in_text` в pipeline:
     ```python
     result = _drop_eyo_substitutions(result, raw_text)
     result = _drop_changes_not_in_text(result, raw_text)
     ```

3. **Тесты** — `tests/test_servers_smoke.py`:
   - `_is_eyo_only_substitution` — 9 кейсов (включая ложно-положительные).
   - `_drop_eyo_substitutions` — 6 кейсов (полная замена, частичная,
     no-op, malformed input, пустой raw_text).
   - Всего 91 ✓ (было 84).

### Прогноз для КС-2 текста v1.6.8

| Параметр | v1.6.7 (factual) | v1.6.8 (прогноз) |
|---|---|---|
| Точные правки | 2/4 | **2-3/4** (может появиться +1 от запятой; причастные обороты сохраняем) |
| Галлюцинации в выходе | 3 (Проведёнными, повлёкших, Подразделению) | **1** (Подразделению) — е/ё фильтруются на сервере |
| Latency | 87 с | **65-75 с** (-12-22 с от сжатия промпта) |

`Подразделения → Подразделению` остаётся открытой проблемой —
требует морфологии или ручного списка стоп-слов. Вынесено в
дорожную карту v1.6.9.

### Действия на проде

```bash
git pull && sudo systemctl restart ai-suggester
# Прогнать те же два теста (КС-2 и Росгвардии)
```

В логах `Дроп стилистической ё-замены: «X» → «Y» (откат в CORRECTED)`
теперь видны ё-фильтрации.

### Откат

Файл `server/local/main.py` — функция `_drop_eyo_substitutions` и её
вызов в pipeline. SYSTEM_PROMPT — git revert до v1.6.7. Можно
отключить только сервер-фильтр: закомментировать вызов
`result = _drop_eyo_substitutions(...)` — фильтрация перестанет
работать, остальное v1.6.7-поведение сохранится.

## v1.6.8 — прод-валидация и обнаруженные проблемы

**Прод-тест 5 мая 2026, ext, T-lite-it-2.1:q4_K_M, КС-2 текст:**

| Запуск | Latency |
|---|---|
| v1.6.7 (cold) | 87 с |
| v1.6.8 cold (после рестарта, 1-й запрос) | 100 с |
| v1.6.8 warm run1 (повтор 5 мин позже) | 96 с |
| v1.6.8 warm run2 (back-to-back) | **79 с** (server dur=77.9 с) |

Warm latency v1.6.8 = **79 с**, на 8 с быстрее v1.6.7. Сжатие
SYSTEM_PROMPT с 2680 до 1631 chars дало эффект, как и обещал.
Cold ~100 с — артефакт прогрева Ollama (kv-cache переинициализация
при разном `num_ctx`), это отдельный диагноз.

**Точность 2/4 стабильна между запусками:** число+причастие в
«стоимостей выполненной» и `Подразделения→Подразделению`. Между
run1 и run2 при этом CHANGES кардинально разные (run1: 1 detailed
compound пункт; run2: 3 diff-реконструированных «без точной причины»).
Это симптом недетерминированности модели (вероятно `temperature>0` в
Ollama options).

**Открытие — bypass `_drop_eyo_substitutions`:**

В run1 модель отдала **compound quote**:
```
«повлекших риски причинения ущерба Подразделения»
→ «повлёкших риски причинения ущерба Подразделению»
```

Внутри ОДНОЙ цитаты — и ё-подмена (повлекших→повлёкших), и
падежная (Подразделения→Подразделению). `_drop_eyo_substitutions`
проверяет «отличается ли before от after ТОЛЬКО ё↔е» — здесь нет,
поэтому фильтр НЕ срабатывает, и в CORRECTED остаётся «повлёкших».

**Подтверждено: модель «не указала причину» в run2** — все 3 пункта
diff-реконструированы, потому что все исходные пункты модели
отфильтрованы (`_drop_changes_not_in_text` дропнул их за неполные
цитаты или галлюцинацию).

## v1.6.9 — Char-level откат ё→е + восстановление прецедента + детерминированность RRF

**Стратегия:** закрываем bypass пост-обработкой CORRECTED ПО СИМВОЛАМ
через difflib (независимо от того, как модель упаковала changes), и
возвращаем компактный конкретный пример «актов … не предусмотренных»
в SYSTEM_PROMPT — он закрывает регресс точности с v1.6.7→v1.6.8.

### Что сделано

1. **`_undo_eyo_in_text(corrected, raw_text)`** в `server/local/main.py`:
   - Выравнивает `raw_text` и `corrected` посимвольно через
     `difflib.SequenceMatcher`.
   - Для каждого `replace`-сегмента с одинаковой длиной с обеих сторон
     посимвольно проверяет: `corrected[j] == 'ё'` И `raw[i] == 'е'` →
     откат к `'е'`. Аналогично для `Ё/Е`.
   - Сегменты разной длины не трогаем — это места реальных правок
     (вставки/удаления символов).
   - Реальная буква ё в raw_text сохраняется (имена типа «Тёркин»).
2. **`_undo_eyo_in_corrected_block(text, raw_text)`** —
   обёртка, применяющая `_undo_eyo_in_text` только к блоку
   `===CORRECTED===` в полном ответе модели, не трогая
   `===CHANGES===`.
3. **Pipeline в suggest():** char-level откат вызывается
   ПОСЛЕ `_drop_eyo_substitutions` (line-level фильтр чистых ё-замен)
   и ДО `_drop_changes_not_in_text` (валидация цитат). Так char-level
   закрывает то, что line-level пропустил из-за compound-цитат.
4. **Восстановлен компактный пример в SYSTEM_PROMPT:**
   *«согласование — однородные члены и причастные обороты в одном
   роде, числе и падеже с главным словом, даже если оно стоит за
   несколько слов («актов, …, не предусмотренных», не
   «предусмотренной»)»* (+55 chars vs v1.6.8). Целевая правка:
   `не предусмотренной → не предусмотренных` (потерянная в v1.6.8).
5. **Детерминированный tie-break в RRF** (`server/shared/gec_bank.py`):
   - Замена `for idx in candidates: ...` → `for idx in sorted(candidates)`.
   - Замена `fused.sort(key=lambda x: x[0], reverse=True)` →
     `fused.sort(key=lambda x: (-x[0], x[1]))`.
   - При равном RRF-скоре теперь стабильно выигрывает пара с
     меньшим индексом в банке. Закрывает наблюдение из v1.6.7→v1.6.8
     (одинаковый текст после рестарта возвращал разные top-1).
6. **Тесты** — 9 новых:
   - `_undo_eyo_in_text`: 7 кейсов (простой, compound bypass, имя
     с ё в raw, нет ё в corrected, верхний регистр, пустой raw).
   - `_undo_eyo_in_corrected_block`: 2 кейса (полный pipeline,
     malformed input).
   - `test_search_hybrid_deterministic_tie_break`: 5 запусков
     одного query, проверка стабильности порядка.
   - Всего 101 ✓ (было 91).

### Прогноз для warm-запуска КС-2

| Параметр | v1.6.8 (factual) | v1.6.9 (прогноз) |
|---|---|---|
| Точные правки | 2/4 | **2-3/4** (вернётся `не предусмотренной`) |
| Галлюцинации | 1 (Подразделению) + 0-1 ё в compound bypass | **0-1** (ё закрыт; Подразделению остаётся) |
| Latency warm | 79 с | **80-83 с** (+1-2 с от примера в промпте) |

Cold-старт ~95-100 с останется — не лечится в этом PR.

### Действия на проде

```bash
git pull && sudo systemctl restart ai-suggester
# Прогнать те же два теста (КС-2 и Росгвардии) ДВАЖДЫ подряд
# для воспроизводимости и стабильности latency
```

В логах теперь видны:
- `Откат ё→е в CORRECTED: N символ(ов) восстановлено по raw_text` —
  char-level фильтр сработал (для compound bypass).
- `Дроп стилистической ё-замены: «X» → «Y»` — line-level фильтр (для
  чистых ё-замен).
- `Few-shot (hybrid): подмешиваю N пар: [...]` — теперь между
  рестартами должны быть стабильны.

### Откат

- Соft rollback char-level: закомментировать `result =
  _undo_eyo_in_corrected_block(...)` — line-level фильтр и остальное
  поведение сохранятся.
- Soft rollback примера: убрать «(«актов, …, не предусмотренных»…)»
  из строки `согласование — …`.
- Soft rollback tie-break: вернуть `for idx in candidates` и
  `key=lambda x: x[0], reverse=True` в `gec_bank.py`.

## v1.6.10 — Реалистичный прогрев + детерминированный сэмплинг

**Дата:** 5 мая 2026 (продолжение)
**Триггеры:** v1.6.8 ablation на КС-2 (Подразделение тест, jascresrq):
- run1 (cold) = 96 с, run2 (warm) = 79 с — 17-21 с разницы между
  cold и warm стабильно повторяется и при v1.6.9 деплое.
- run1 vs run2 на одном тексте дают **разные CHANGES** (run1 — один
  detailed compound-пункт, run2 — три пункта diff-reconstruction).
  Модель недетерминирована при temperature=0.1.
- Жёсткое ограничение пользователя (verbatim, 5 мая 2026):
  *«По latency мы уже говорили что максимум это 80 сек. на проде
  дольше это уже не исправитель текста, а вор времени на составление
  документов.»* + *«GPU не доступна, нет других серверов и средств
  на внешние gpu.»*

### Корни проблем

**1. Прогрев с `num_ctx=512`, реальные запросы с `num_ctx=2048-4096`.**
До v1.6.10 функция `_warmup_ollama` слала минимальный chat-запрос
(`"ok"`, `num_ctx=512`). Ollama при первом реальном запросе с длинным
промптом вынуждена **переаллоцировать kv-cache** под новый размер:
освободить старые буферы, выделить новые, переинициализировать
attention-pattern. На CPU это +15-25 с — ровно тот разрыв, который
наблюдался между cold (96 с) и warm (79 с).

Issue [ollama/ollama#2431](https://github.com/ollama/ollama/issues/2431)
подтверждает: пустой запрос на `/api/chat` действительно загружает
веса в RAM, но **не аллоцирует cache на нужный размер** — это делает
первый реальный запрос. Решение — слать прогрев с реалистичным
`num_ctx` и достаточной длиной промпта.

**2. `temperature=0.1` хардкод в `call_ollama`.** Даже малая
температура даёт вариативность вывода между запусками, что:
- маскирует регрессии в ручных QA-прогонах (одинаковый вход → разный
  выход → невозможно повторить ошибку);
- ломает воспроизводимость debug-сессий;
- создаёт впечатление «модель то ловит ошибку, то нет», хотя реально
  это сэмплинг в области малой неопределённости логитов.

GEC — task с единственным верным ответом для большинства токенов,
sampling здесь не нужен. Greedy (temperature=0) + repeat_penalty уже
дают качество без вариативности.

### Изменения

**`server/local/main.py`:**

1. Новая env var `OLLAMA_TEMPERATURE` (дефолт `0`). Заменяет хардкод
   `0.1` в `call_ollama` payload. Поднять до 0.1-0.3 можно для
   exploration в исследовательских прогонах, но дефолт — детерминизм.

2. `_warmup_ollama` переписан под realistic-warmup:
   - `num_ctx=OLLAMA_NUM_CTX` (вместо `512`) → kv-cache аллоцируется
     сразу под продовый размер.
   - User-промпт ~1700 chars (вместо `"ok"`) — Ollama прогревает
     attention-pattern и BLAS-ядра именно под русский текст и
     длинный input.
   - `num_predict=8` (вместо `1`) — прогревается decode-loop, а не
     только prefill.
   - `temperature=OLLAMA_TEMPERATURE` — sampling-state совпадает с
     реальными запросами.

3. `call_ollama` payload: `"temperature": 0.1` → `"temperature":
   OLLAMA_TEMPERATURE`.

**`server/local/.env.example`:** добавлена `OLLAMA_TEMPERATURE=0` с
комментариями про v1.6.8 ablation.

**`tests/test_servers_smoke.py`:** +6 тестов (всего 107):
- `test_local_warmup_uses_full_num_ctx`: warmup передаёт
  `num_ctx=OLLAMA_NUM_CTX`.
- `test_local_warmup_passes_temperature`: warmup передаёт
  `temperature` явно, и оно совпадает с `OLLAMA_TEMPERATURE`.
- `test_local_warmup_uses_realistic_prompt`: длина промпта
  `>=500` chars.
- `test_local_warmup_skipped_when_disabled`: `OLLAMA_WARMUP=false`
  не делает HTTP-запросов.
- `test_local_call_ollama_default_temperature_zero`: дефолт `0`
  доходит до Ollama.
- `test_local_call_ollama_respects_temperature_override`: env
  override (0.35) пробрасывается без изменений.

### Прогноз по latency и качеству

| Параметр | v1.6.9 (prod) | v1.6.10 (прогноз) |
|---|---|---|
| Cold-старт КС-2 | 95-100 с | **~80 с** (как warm) |
| Warm КС-2 | 79-80 с | **79-80 с** (без изменений) |
| Детерминизм run1==run2 | НЕТ (CHANGES разные) | **ДА** (greedy) |
| Точность КС-2 | 2-3/4 | **2-3/4 стабильно** |

Двойной прогон для warmup перестаёт быть нужным — single-shot тест
работает корректно с первого раза. Это и было запросом пользователя.

### Что НЕ вошло (откладывается на v1.7)

Пользователь явно выбрал последовательность: сначала v1.6.10
(warmup+temperature), потом v1.7 «Путь B» (расширение банка +
char-trigram BM25 + pymorphy3). Все эти три направления — pure Python,
без новых зависимостей кроме pymorphy3, без GPU, latency-нейтральны.

## Что впереди (после v1.6.10)

Список улучшений в порядке убывания ROI:

1. **v1.7 «Путь B» — расширение GEC-банка через RuGECv1.** Фильтрация
   707 261 пары на админ-стиль → 5-10K пар, char-trigram BM25 в
   `gec_bank.py` для морфологического матчинга, pymorphy3 фильтр
   падежных «улучшений». Закроет последний открытый класс галлюцинаций
   (Подразделению) и резко улучшит retrieval coverage по результатам
   Sorokin & Nasyrova BEA 2025 («retrieval quality dominates LLM-GEC
   quality»). Latency: +1-2 мс на pymorphy3 + 5-10 мс на trigram,
   итого ≤80 с warm.
2. **v1.8 (опционально) — LanguageTool-RU как пост-валидатор** или
   **GECTOR-Russian encoder.** Только если v1.7 не достигнет 4/4 на
   КС-2. Latency: +200-500 мс (LanguageTool, Java daemon) или
   +50-200 мс (GECTOR, BERT-RU encoder).
3. **Расширение корпусами** — RU-Lang8, MultiGED-RU, RuCoLA-derived
   через тот же препроцессор RuGECv1.
4. **Phase 3 ингест** — реальные приказы из Гарант/Консультанта.

**Отвергнуто пользователем (verbatim, 5 мая 2026):**
- *«удлинять latency в любом варианте плохой путь»* — отказ от
  two-pass / self-critique, GEC_TOP_K=2 и любых других вариантов,
  стоящих секунд latency. Все будущие PR должны быть
  latency-нейтральны или сокращающими.
- *«По latency мы уже говорили что максимум это 80 сек.»* — жёсткий
  потолок warm latency. v1.7 «Путь B» помещается, v1.8 LanguageTool —
  на грани (нужен бенчмарк на проде перед релизом).
- *«GPU не доступна, нет других серверов и средств на внешние gpu.»*
  — отказ от LoRA fine-tune (Path C, требовал rented GPU $5-15) и от
  смены модели на 12B+ (Path B, +50-100% latency).


## v1.7 — Путь B (pymorphy3 + char-trigram BM25)

См. [PR #37](https://github.com/Jadessswq/MainCheck/pull/37). Описание,
тесты, прогноз latency/качества и инструкция отката — в теле PR.
Часть 3/3 (расширение GEC-банка) отложена, причины тоже в PR.


## v1.7.1 — фикс morph-фильтра на compound-цитатах

См. [PR #38](https://github.com/Jadessswq/MainCheck/pull/38). Морф-фильтр
(v1.7) пропускал prod-кейс «...Подразделения» → «...Подразделению»
из-за многословной цитаты модели; v1.7.1 добавляет compound-аналог:
word-by-word анализ внутри цитаты, откат галлюцинированных слов в CORRECTED.


## v1.7.2 — расширение GEC-банка через alexanderpl/Lexify_RuGEC

См. [PR #39](https://github.com/Jadessswq/MainCheck/pull/39). Корпус-экспансия (часть 3/3 v1.7, разблокирована альтернативным
открытым датасетом). Банк 927 → 2550 пар (+1623 морфо-агременты:
case_agreement / number_agreement / verb_form / case_and_number).
Закрывает recall-дыру на согласовании числа подлежащего/сказуемого и
числе после порядковых числительных.


## v1.7.3 — контекстная проверка adj-noun агремента в morph-фильтре + пере-нумерация CHANGES

См. PR. Фикс false-positive v1.7.1 morph-фильтра: на «Проверочное мероприятия → мероприятие»
pymorphy3 даёт амбигюусные парсы, best-парс трактует как case-only sub и откатывает legitimate number-fix.
v1.7.3 проверяет согласование `before` с предыдущим adj/прич/мест и не откатывает рассогласованные.
Дополнительно: `_renumber_changes` пере-нумеровывает CHANGES после drop'ов, закрывает кейс «правки начались со 2-го пункта».


## v1.8 — pymorphy3 morph-детектор + пользовательский словарь

См. [PR #41](https://github.com/Jadessswq/MainCheck/pull/41). Архитектурный
скачок вместо точечных правок: детектор грамматических ошибок (numeral-noun,
adj-noun, OOV) на pymorphy3 + пользовательский словарь с REST API и UI-кнопкой
в LibreOffice-расширении. Описание, тесты, прогноз latency/качества и
инструкция отката — в теле PR.


## v1.8.1 — фикс FP детектора + Basic-синтаксис Dict.xba

См. PR. Два prod-фикса поверх v1.8: (1) FP «помещений → помещения» в
adj_noun-детекторе при OOV-соседе («ремонтова»); (2) Basic-синтаксическая
ошибка «Неожиданный символ: (.» в Dict.xba (несовместимые `As String()`
и отрицательные `ReDim`). Детали — в теле PR.


## v1.8.2 — фикс UI словаря и идемпотентного фильтра с вложенными кавычками

См. PR. Два прод-фикса поверх v1.8.1: (1) Dict.xba врала об ошибке когда
status-файл curl остался пустым — теперь fallback на JSON-маркеры тела;
(2) `_drop_idempotent_changes` пропускал пункты вида «X «Y»» → «X «Y»»
(вложенные кавычки ломали non-greedy regex). Детали — в теле PR.


## v1.8.3 — диалог управления словарём («AI: Словарь»)

См. PR. Новая кнопка m003 на панели + Sub `Dict.AIDictManage` строит UnoControlDialog
со списком слов и кнопками Удалить/Добавить/Закрыть. Детали в теле PR.


## v1.8c — sage-95m post-валидатор (опциональный, default OFF)

См. PR. Pluggable post-фильтр CHANGES через `ai-forever/sage-fredt5-distilled-95m`,
default OFF и dryrun. Детали в теле PR.
